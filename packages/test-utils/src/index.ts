/**
 * @posx/test-utils
 *
 * Phase 6 will populate this package with:
 *   - deterministic wallet-address / uuid / date builders
 *   - fixture factories for users, purchases, referral trees, reward
 *     snapshots, burn records, claim orders, and adjustments
 *   - in-memory repositories that implement the backend repo interfaces
 *   - a `FixedClock` preset re-exported from `@posx/shared-utils`
 *
 * Intended consumers: unit tests, integration tests, and any E2E smoke
 * harness that needs to boot without a real database.
 */
export {};
