# @posx/admin-web

Placeholder for Phase 5.

**Do not add application code here in Phase 2/3/4.** This workspace slot is
reserved so that `pnpm install` recognises the package and linkages remain
stable across phases.

When Phase 5 begins, this app will be scaffolded with:

- Vite + React 18 + TypeScript 5
- `antd` + `@ant-design/pro-components`
- `@tanstack/react-query`
- `dayjs`
- `zod` (consumed via `@posx/api-contracts`)
- `decimal.js` (consumed via `@posx/shared-utils`)

See `06_admin_panel_prd.md` at the repo root for the full page and
permission spec.
